# Docker container dependencies for MCP server
# FastMCP 2.x supports Streamable HTTP transport (required for APIM native MCP support)
fastmcp>=2.0.0
pydantic>=2.0.0
uvicorn[standard]
httpx
python-dotenv
starlette
